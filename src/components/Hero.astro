<section class="hero">
  <div class="container">
    <div class="hero-top">
      <div class="hero-text">
        <div class="kicker">Editorial & Reviewer Companion</div>
        <h1 class="hero-title">Same scaffold,<br/>different controllers.</h1>
        <p class="hero-lead">Across five phases of the COVID-19 pandemic, youth symptom networks preserved a broadly similar modular architecture while the distribution of control shifted from an early stress-centered configuration toward a later cross-domain pattern.</p>
        <div class="hero-actions">
          <a class="button" href="#summary">Read the summary</a>
          <a class="button alt" href="#phases">Explore phases</a>
          <a class="button alt" href="#methods">Methods</a>
        </div>
      </div>
      <figure class="hero-fig fig-inline">
        <img src="/covid-mcn-site/figs/fig1.png" alt="Study overview: pipeline from network estimation to module control" loading="eager" />
        <figcaption class="fig-cap">Fig. 1 — Analytic pipeline overview</figcaption>
      </figure>
    </div>
    <div class="hero-bar">
      <div class="hero-stats">
        <div class="stat"><strong>14,181</strong> <span>U.S. young adults</span></div>
        <div class="stat"><strong>47</strong> <span>MHQ symptoms</span></div>
        <div class="stat"><strong>5</strong> <span>pandemic phases</span></div>
        <div class="stat"><strong>4</strong> <span>symptom domains</span></div>
      </div>
      <div class="hero-domains">
        <span class="dpill"><span class="domain-dot" style="background: var(--str)"></span>Stress Response</span>
        <span class="dpill"><span class="domain-dot" style="background: var(--emo)"></span>Emotional Regulation</span>
        <span class="dpill"><span class="domain-dot" style="background: var(--csf)"></span>Cognitive & Social</span>
        <span class="dpill"><span class="domain-dot" style="background: var(--spf)"></span>Self-perception & Physiological</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: clamp(40px, 5vw, 60px) 0 clamp(24px, 3vw, 36px);
    border-bottom: 1px solid var(--line);
  }

  .hero-top {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: clamp(24px, 4vw, 48px);
    align-items: center;
    margin-bottom: clamp(20px, 3vw, 32px);
  }

  .hero-title {
    font-size: clamp(1.8rem, 4vw, 3.2rem);
    line-height: 1.08;
    margin-bottom: 16px;
    letter-spacing: -0.02em;
  }

  .hero-lead {
    font-size: clamp(0.9rem, 1vw, 1.02rem);
    color: var(--muted);
    max-width: 52ch;
    margin-bottom: 24px;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .hero-fig {
    margin: 0;
  }

  .hero-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    padding-top: clamp(16px, 2vw, 24px);
    border-top: 1px solid var(--line);
    flex-wrap: wrap;
  }

  .hero-stats {
    display: flex;
    gap: clamp(12px, 2vw, 24px);
    flex-wrap: wrap;
  }

  .stat {
    display: flex;
    align-items: baseline;
    gap: 6px;
  }

  .stat strong {
    font-family: var(--font-display);
    font-size: clamp(1.1rem, 1.5vw, 1.4rem);
    font-weight: 700;
  }

  .stat span {
    font-family: var(--font-mono);
    font-size: clamp(0.65rem, 0.7vw, 0.72rem);
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .hero-domains {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .dpill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 5px 12px;
    border-radius: 999px;
    border: 1px solid var(--line);
    background: var(--paper);
    font-size: clamp(0.74rem, 0.85vw, 0.84rem);
    font-weight: 500;
    white-space: nowrap;
  }

  @media (max-width: 768px) {
    .hero-top {
      grid-template-columns: 1fr;
      gap: 20px;
    }
    .hero-bar { flex-direction: column; align-items: flex-start; }
    .hero-actions { width: 100%; }
    .hero-actions .button { flex: 1; justify-content: center; min-width: 120px; }
  }
</style>
