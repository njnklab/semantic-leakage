<section id="glossary">
  <div class="container">
    <div class="section-head">
      <div class="kicker">Glossary</div>
      <h2>Key terms, plainly explained.</h2>
    </div>
    <div class="gloss-grid stagger">
      <div class="gloss-item">
        <div class="gloss-term">Mesoscale scaffold</div>
        <p>The relatively stable community structure of the symptom network — the broad modular organization that persists even when influence shifts across phases.</p>
      </div>
      <div class="gloss-item">
        <div class="gloss-term">Minimum dominating set <span class="abbr">MDS</span></div>
        <p>The smallest set of nodes that covers the whole graph directly or through neighbors. Defines which symptoms can serve as drivers.</p>
      </div>
      <div class="gloss-item">
        <div class="gloss-term">Control frequency <span class="abbr">CF</span></div>
        <p>Proportion of exact minimum dominating sets in which a symptom appears. High CF = repeated participation in driver solutions.</p>
      </div>
      <div class="gloss-item">
        <div class="gloss-term">Avg. control frequency <span class="abbr">ACF</span></div>
        <p>Mean normalized CF within a module. Informative descriptively, but less stable under case loss than AMCS.</p>
      </div>
      <div class="gloss-item">
        <div class="gloss-term">Avg. module control strength <span class="abbr">AMCS</span></div>
        <p>Directed summary of how strongly one module dominates another. The manuscript's key mesoscale metric.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .gloss-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 2px;
    border-radius: var(--radius);
    overflow: hidden;
    border: 1px solid var(--line);
  }

  .gloss-item {
    padding: clamp(14px, 2vw, 20px);
    background: var(--paper);
  }

  .gloss-term {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(0.86rem, 0.96vw, 0.95rem);
    margin-bottom: 8px;
  }

  .abbr {
    font-family: var(--font-mono);
    font-size: clamp(0.64rem, 0.72vw, 0.72rem);
    font-weight: 500;
    color: var(--muted);
    background: var(--bg);
    border: 1px solid var(--line);
    padding: 2px 6px;
    border-radius: 4px;
    margin-left: 4px;
    vertical-align: middle;
  }

  .gloss-item p {
    color: var(--muted);
    font-size: clamp(0.78rem, 0.86vw, 0.86rem);
    margin-bottom: 0;
    line-height: 1.6;
  }
</style>
