<section id="resources">
  <div class="container-narrow">
    <div class="section-head">
      <div class="kicker">Resources</div>
      <h2>What to read next.</h2>
    </div>
    <div class="res-list">
      <a class="panel res-item" href="#">
        <div class="res-icon">PDF</div>
        <div class="res-info">
          <strong>Main manuscript</strong>
          <span>Full article for formal review.</span>
        </div>
        <span class="res-arrow">&rarr;</span>
      </a>
      <a class="panel res-item" href="#">
        <div class="res-icon">SI</div>
        <div class="res-info">
          <strong>Supplementary information</strong>
          <span>Penalty sensitivity, variable mapping, additional robustness.</span>
        </div>
        <span class="res-arrow">&rarr;</span>
      </a>
      <a class="panel res-item" href="https://github.com/realfty/covid-mcn">
        <div class="res-icon">&lt;/&gt;</div>
        <div class="res-info">
          <strong>Analysis code</strong>
          <span>Full pipeline on GitHub.</span>
        </div>
        <span class="res-arrow">&rarr;</span>
      </a>
    </div>
  </div>
</section>

<style>
  .res-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .res-item {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: clamp(14px, 2vw, 18px) clamp(14px, 2vw, 20px);
    transition: all 0.2s ease;
    color: var(--ink);
  }

  .res-item:hover {
    transform: translateX(4px);
    text-decoration: none;
    border-color: var(--ink);
  }

  .res-icon {
    width: 40px;
    height: 40px;
    border-radius: 10px;
    background: var(--bg);
    border: 1px solid var(--line);
    display: grid;
    place-items: center;
    font-family: var(--font-mono);
    font-size: clamp(0.66rem, 0.75vw, 0.74rem);
    font-weight: 700;
    color: var(--muted);
    flex-shrink: 0;
  }

  .res-info { flex: 1; }
  .res-info strong { display: block; font-size: clamp(0.86rem, 0.95vw, 0.94rem); margin-bottom: 2px; }
  .res-info span { font-size: clamp(0.78rem, 0.86vw, 0.86rem); color: var(--muted); }

  .res-arrow {
    font-size: 1.1rem;
    color: var(--muted);
    transition: transform 0.2s, color 0.2s;
  }

  .res-item:hover .res-arrow { transform: translateX(4px); color: var(--ink); }
</style>
