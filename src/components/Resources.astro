<section id="resources">
  <div class="container-narrow">
    <div class="section-head">
      <div class="kicker">Resources</div>
      <h2>What to read next.</h2>
    </div>
    <div class="resource-list">
      <a class="panel resource-item" href="#">
        <div class="resource-icon">PDF</div>
        <div class="resource-info">
          <strong>Main manuscript</strong>
          <span>Full article for formal review.</span>
        </div>
        <span class="resource-arrow">&rarr;</span>
      </a>
      <a class="panel resource-item" href="#">
        <div class="resource-icon">SI</div>
        <div class="resource-info">
          <strong>Supplementary information</strong>
          <span>Penalty sensitivity, variable mapping, additional robustness.</span>
        </div>
        <span class="resource-arrow">&rarr;</span>
      </a>
      <a class="panel resource-item" href="https://github.com/realfty/covid-mcn">
        <div class="resource-icon">&lt;/&gt;</div>
        <div class="resource-info">
          <strong>Analysis code</strong>
          <span>Full pipeline on GitHub.</span>
        </div>
        <span class="resource-arrow">&rarr;</span>
      </a>
    </div>
  </div>
</section>

<style>
  .resource-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .resource-item {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 18px 20px;
    transition: all 0.2s ease;
    color: var(--ink);
  }

  .resource-item:hover {
    transform: translateX(4px);
    text-decoration: none;
    border-color: var(--ink);
  }

  .resource-icon {
    width: 44px;
    height: 44px;
    border-radius: 10px;
    background: var(--bg);
    border: 1px solid var(--line);
    display: grid;
    place-items: center;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--muted);
    flex-shrink: 0;
  }

  .resource-info {
    flex: 1;
  }

  .resource-info strong {
    display: block;
    font-size: 0.92rem;
    margin-bottom: 2px;
  }

  .resource-info span {
    font-size: 0.84rem;
    color: var(--muted);
  }

  .resource-arrow {
    font-size: 1.2rem;
    color: var(--muted);
    transition: transform 0.2s, color 0.2s;
  }

  .resource-item:hover .resource-arrow {
    transform: translateX(4px);
    color: var(--ink);
  }
</style>
