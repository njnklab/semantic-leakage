<section id="summary">
  <div class="container">
    <div class="section-head">
      <div class="kicker">30-Second Summary</div>
      <h2>Four signals that define this paper.</h2>
      <p class="subtitle">What is being asked, what evidence is available, what methodological advance is introduced, and what conclusion is justified.</p>
    </div>
    <div class="summary-layout">
      <div class="signal-grid stagger">
        <div class="panel signal-card">
          <div class="signal-num">01</div>
          <h3>Question</h3>
          <p>Did the pandemic reorganize the architecture of youth symptoms, or did it mainly reallocate control within a conserved symptom scaffold?</p>
        </div>
        <div class="panel signal-card">
          <div class="signal-num">02</div>
          <h3>Evidence</h3>
          <p>Repeated cross-sections of U.S. young adults aged 18–24 across five pandemic phases, with 47 MHQ symptom items and an India replication cohort.</p>
        </div>
        <div class="panel signal-card">
          <div class="signal-num">03</div>
          <h3>Advance</h3>
          <p>The paper models domination-based control at the module level and tests which derived metrics remain interpretable under resampling.</p>
        </div>
        <div class="panel signal-card">
          <div class="signal-num">04</div>
          <h3>Conclusion</h3>
          <p>Modules remain broadly stable, but control shifts from early stress dominance to later multi-module governance. AMCS is more reliable than ACF.</p>
        </div>
      </div>
      <div class="summary-figure figure-block">
        <img src="/covid-mcn-site/figs/fig2.png" alt="Fig. 2 — Symptom network structure across five pandemic phases" loading="lazy" />
        <div class="figure-caption">Fig. 2 — Symptom network structure and community detection across five pandemic phases</div>
      </div>
    </div>
  </div>
</section>

<style>
  .summary-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    align-items: start;
  }

  .signal-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }

  .signal-card {
    padding: 20px;
  }

  .signal-num {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--muted);
    letter-spacing: 0.1em;
    margin-bottom: 12px;
    font-weight: 500;
  }

  .signal-card h3 {
    font-size: 1.1rem;
    margin-bottom: 8px;
  }

  .signal-card p {
    color: var(--muted);
    font-size: 0.9rem;
    margin-bottom: 0;
    line-height: 1.6;
  }

  .summary-figure {
    box-shadow: var(--shadow);
    position: sticky;
    top: 80px;
  }

  @media (max-width: 768px) {
    .summary-layout { grid-template-columns: 1fr; }
    .signal-grid { grid-template-columns: 1fr; }
    .summary-figure { position: static; }
  }
</style>
