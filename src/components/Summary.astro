<section id="summary">
  <div class="container">
    <div class="section-head">
      <div class="kicker">30-Second Summary</div>
      <h2>Four signals that define this paper.</h2>
    </div>
    <div class="summary-flow">
      <div class="signal-strip stagger">
        <div class="signal">
          <span class="snum">01</span>
          <h4>Question</h4>
          <p>Did the pandemic reorganize the architecture of youth symptoms, or mainly reallocate control within a conserved scaffold?</p>
        </div>
        <div class="signal">
          <span class="snum">02</span>
          <h4>Evidence</h4>
          <p>Repeated cross-sections of U.S. young adults aged 18–24 across five pandemic phases, with 47 MHQ items and an India replication cohort.</p>
        </div>
        <div class="signal">
          <span class="snum">03</span>
          <h4>Advance</h4>
          <p>Models domination-based control at the module level and tests which derived metrics remain interpretable under resampling.</p>
        </div>
        <div class="signal">
          <span class="snum">04</span>
          <h4>Conclusion</h4>
          <p>Modules remain broadly stable, but control shifts from early stress dominance to later multi-module governance. AMCS is more reliable than ACF.</p>
        </div>
      </div>
      <div class="summary-figs">
        <figure class="fig-inline">
          <img src="/covid-mcn-site/figs/fig2-1.png" alt="Symptom networks across five phases with community structure and bootstrap stability" loading="lazy" />
          <figcaption class="fig-cap">Fig. 2 — Symptom networks, community structure and bootstrap stability across phases</figcaption>
        </figure>
      </div>
    </div>
  </div>
</section>

<style>
  .summary-flow {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(16px, 3vw, 32px);
    align-items: start;
  }

  .signal-strip {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }

  .signal {
    padding: clamp(14px, 2vw, 20px);
    border-radius: var(--radius);
    background: var(--paper);
    border: 1px solid var(--line);
  }

  .snum {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    color: var(--muted);
    letter-spacing: 0.1em;
    margin-bottom: 8px;
    display: block;
    font-weight: 500;
  }

  .signal h4 {
    font-family: var(--font-display);
    font-size: clamp(0.95rem, 1.1vw, 1.1rem);
    margin-bottom: 6px;
  }

  .signal p {
    color: var(--muted);
    font-size: clamp(0.82rem, 0.9vw, 0.9rem);
    margin-bottom: 0;
    line-height: 1.6;
  }

  .summary-figs {
    position: sticky;
    top: 72px;
  }

  @media (max-width: 768px) {
    .summary-flow { grid-template-columns: 1fr; }
    .signal-strip { grid-template-columns: 1fr; }
    .summary-figs { position: static; }
  }
</style>
