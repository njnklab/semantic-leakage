<section id="limitations">
  <div class="container-narrow">
    <details class="panel limit-box">
      <summary>
        <span class="limit-title">What this paper does not claim</span>
        <span class="limit-toggle">+</span>
      </summary>
      <div class="limit-content">
        <div class="limit-grid">
          <div class="limit-item">
            <h4>Not within-person longitudinal</h4>
            <p>Repeated cross-sections, not a cohort tracked over time. Phase comparisons describe population-level shifts, not individual trajectories.</p>
          </div>
          <div class="limit-item">
            <h4>Edges ≠ causal direction</h4>
            <p>Conditional dependencies from cross-sectional data. They do not imply that one symptom causes another.</p>
          </div>
          <div class="limit-item">
            <h4>Policy associations are exploratory</h4>
            <p>Macro-level analyses linking symptoms to policy variables are hypothesis-generating, not confirmatory.</p>
          </div>
          <div class="limit-item">
            <h4>Domain mapping is interpretive</h4>
            <p>The four-domain alignment is a fixed interpretive layer for cross-phase comparability, not an ontological claim.</p>
          </div>
        </div>
      </div>
    </details>
  </div>
</section>

<style>
  #limitations { padding-top: 0; padding-bottom: 32px; }

  .limit-box { overflow: hidden; }

  .limit-box summary {
    list-style: none;
    cursor: pointer;
    padding: clamp(14px, 2vw, 20px) clamp(16px, 2vw, 24px);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .limit-box summary::-webkit-details-marker { display: none; }

  .limit-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(0.92rem, 1.05vw, 1.05rem);
  }

  .limit-toggle {
    font-family: var(--font-mono);
    font-size: 1.2rem;
    color: var(--muted);
    transition: transform 0.2s;
  }

  .limit-box[open] .limit-toggle { transform: rotate(45deg); }

  .limit-content { padding: 0 clamp(16px, 2vw, 24px) clamp(16px, 2vw, 24px); }

  .limit-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }

  .limit-item {
    padding: clamp(12px, 1.5vw, 16px);
    border-radius: var(--radius);
    background: var(--bg);
    border: 1px solid var(--line);
  }

  .limit-item h4 { font-size: clamp(0.8rem, 0.88vw, 0.88rem); margin-bottom: 6px; }
  .limit-item p { font-size: clamp(0.78rem, 0.86vw, 0.86rem); color: var(--muted); margin-bottom: 0; line-height: 1.55; }

  @media (max-width: 768px) {
    .limit-grid { grid-template-columns: 1fr; }
  }
</style>
